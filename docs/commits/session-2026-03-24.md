# Commit Message — Session 2026-03-24

## Terminal Command (paste directly)

```bash
git add -A -- ':!.DS_Store' && git commit -m "feat: separate SDK app users from admin users, add RBAC guard chain, org-member access controls, and AppSelector across Roles/Permissions/Users pages

Introduces a clean architectural separation between Heimdal admin users
(invite-gated, OrgMembership) and SDK-registered app users (AppMembership,
no admin access):

DB
- Add AppMembership model — end-users registered per-app via SDK; cascades on
  User/Application delete; @@unique([userId, appId])
- Rename parentRoleId → baseRoleId / childRoles → derivedRoles for semantic clarity

API — new SDK module (/sdk/:appId/*)
- AppSecretGuard validates X-App-Secret against stored SHA-256 hash
- Register, login, refresh endpoints for app users — no invite required
- JwtAuthGuard rejects any token where aud.startsWith('app_') on all admin routes
- OrgMembershipGuard + @OrgWriteAccess() blocks org members from mutating entitlements
- HeimdalRolesGuard + @OrgScoped() guard chain enforces org boundaries end-to-end
- Members endpoints: GET/PATCH/DELETE admin/orgs/:orgId/members; role changes
  locked to platform admins only; self-edit prevention in backend and UI

Admin UI
- AppContext + AppSelector section component: app-scoped state persisted per-org
  in localStorage; read-only pill for bound org members
- Users page: tabbed Org Members / App Users view; App Users tab driven by AppSelector
- Roles/Permissions pages: AppSelector inline with page header; CreateRoleModal
  accepts prefilled org+app context; read-only mode for org members
- Sidebar + router: org members land on /roles, see only Roles/Permissions/Profile;
  all other routes redirect via NotForOrgMember guard component
- useFeatureAccess() gates all destructive UI actions; useSession() derives
  isOrgMember, isOrgAdmin, isPlatformAdmin
- Invite flow unified under POST admin/invites; member invites require appId

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Detailed Message Only

```
feat: separate SDK app users from admin users, add RBAC guard chain, org-member access controls, and AppSelector across Roles/Permissions/Users pages

Introduces a clean architectural separation between Heimdal admin users
(invite-gated, OrgMembership) and SDK-registered app users (AppMembership,
no admin access):

DB
- Add AppMembership model — end-users registered per-app via SDK; cascades on
  User/Application delete; @@unique([userId, appId])
- Rename parentRoleId → baseRoleId / childRoles → derivedRoles for semantic clarity

API — new SDK module (/sdk/:appId/*)
- AppSecretGuard validates X-App-Secret against stored SHA-256 hash
- Register, login, refresh endpoints for app users — no invite required
- JwtAuthGuard rejects any token where aud.startsWith('app_') on all admin routes
- OrgMembershipGuard + @OrgWriteAccess() blocks org members from mutating entitlements
- HeimdalRolesGuard + @OrgScoped() guard chain enforces org boundaries end-to-end
- Members endpoints: GET/PATCH/DELETE admin/orgs/:orgId/members; role changes
  locked to platform admins only; self-edit prevention in backend and UI

Admin UI
- AppContext + AppSelector section component: app-scoped state persisted per-org
  in localStorage; read-only pill for bound org members
- Users page: tabbed Org Members / App Users view; App Users tab driven by AppSelector
- Roles/Permissions pages: AppSelector inline with page header; CreateRoleModal
  accepts prefilled org+app context; read-only mode for org members
- Sidebar + router: org members land on /roles, see only Roles/Permissions/Profile;
  all other routes redirect via NotForOrgMember guard component
- useFeatureAccess() gates all destructive UI actions; useSession() derives
  isOrgMember, isOrgAdmin, isPlatformAdmin
- Invite flow unified under POST admin/invites; member invites require appId

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

## Concise

```
feat: separate SDK app users from admin users, add RBAC guard chain, org-member access restrictions, and AppSelector across Roles/Permissions/Users pages
```
