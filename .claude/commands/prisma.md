Read docs/coding-prompts/12-how-to-write-prisma-queries.md before writing any database queries. Key rules: always scope by orgId for tenant isolation, use select for list views, use transactions for multi-step writes, use soft delete pattern (deletedAt) for core entities, and use _count for relation counting. Also reference the Prisma conventions in CLAUDE.md section 6 for schema design rules.