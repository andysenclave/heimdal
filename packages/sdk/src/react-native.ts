/**
 * @heimdal/sdk/react-native
 *
 * React Native integration — HeimdalProvider, useAuth, useGuard hooks.
 * Full implementation in Week 4.
 */

export { HeimdalClient, SDK_VERSION } from './index';
export type { HeimdalConfig } from './index';

// Placeholder — React Native hooks will be built in Week 4
export const REACT_NATIVE_SUPPORTED = true;
